# Archivos de sistema
.DS_Store
Thumbs.db

# Variables de entorno (nunca subir credenciales)
.env
.env.local

# Editores
.vscode/
*.suo
*.user

# Pruebas locales — credenciales reales y salidas de los test (nunca subir)
pruebas/credenciales.local.json
pruebas/salida/

# Espejo de produccion — volcado con datos personales reales (nunca subir)
supabase/espejo/
